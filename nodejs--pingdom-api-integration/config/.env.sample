# Pingdom primary account credentials
PINGDOM_USERNAME=replaceme
PINGDOM_PASSWORD=replaceme

# Pingdom application key for API access
PINGDOM_APP_KEY=replaceme

# Pingdom multi-user account email
PINGDOM_ACCOUNT_EMAIL=replaceme

# Pingdom API endpoint base URL
PINGDOM_API_URL=https://api.pingdom.com/api/2.0
